title: "V-unit test"
suite:
- ident: SCAN
  title: "Scan QR code" 
  steps:
  - command: scan ANY
    extractKey: SerialNum
  - command: serial set %SerialNum%
    retry: 3

- ident: WAIT_FOR_START
  title: "Waiting for operator"
  steps:
  - command: operator "Press start when V-unit is ready for test"
    title: "Confirm device setup"
    timeout: 30s

- ident: Sensor_comparison_test
  steps:
# 1. Measure voltages on reference and internal sensor ##########################################
  - command: power 3V3 on
    title: "Power to DUT enabled !"
  - command: sleepms 1000 #Wait for the PSU to set 3.3V
  - command: measure voltageDATP07 0-3.3V
    extractKey: RefSens_raw
    title: "Measure reference sensor pressure"
  - command: measure voltageDATP08 0-3.3V
    extractKey: IntSens_raw
    title: "Measure internal sensor pressure"


# 2. Convert the measured data to pressure: ##################################################
  - command: eval "numeric(RefSens_raw, 'V')"
    extractKey: RefSens
    title: "Extract value from measurement reference sensor"
  - command: eval "(RefSens -2.7)/0.025"
    extractKey: RefPress
    title: "Convert reference sensor voltage to pressure"
  - command: eval "numeric(IntSens_raw, 'V')"
    extractKey: IntSens
    title: "Extract value from measurement internal sensor"
  - command: eval "(IntSens -2.7)/0.025"
    extractKey: IntPress
    title: "Convert internal sensor voltage to pressure"
    
# 3. Compare difference between measurement > 10 % = FAIL: #########################################
  - command: eval "RefSens/IntSens"
    extractKey: VoltDiff
  - command: eval "VoltDiff >= 0.9 && VoltDiff <= 1.1"
    title: "Output difference <= 10% of reference ?"
    retry: 2
    fail: "Sensor ouput difference exceeds 10 %, check V-unit and sensor connection !"   

# 4. POWER rail control for pump, valve and MCU ###################################################
- ident: POWER_CNTRL
  steps:
  - command: power VARV 16
    title: "16 V power enabled !"
  - command: sleepms 500 #Wait for power to stabilize
  - command: pin DDTP09 output high
    title: "Setting valve to open"

  - command: power 5V on
    title: "Power to MCU enabled !"
    # Add mcu fault check!
  - command: sleepms 500

# 5. Reaching set pressure with different pump PWM duty cycle test #################################
  # 25% Duty cycle
- ident: VALVE and PUMP control on startup 
  steps:
    
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    
    - command: pin DDTP11 output high #send signal to MCU to enable pump
    - command: wait
      timeout: 100

    - command: pin DDTP10 output high
    - command: wait
      timeout: 100
    - command: pin DDTP10 output low
      title: "Increment PWM duty (MCU triggered)"
    - command: wait
      timeout: 100

- ident: Target pressure reached within time frame test
  steps:
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_25
    - command: eval "(vacuum_25-2.7)/0.025"
      extractKey: curr_press_25
    - command: eval "((vacuum_25-2.7)/0.025) >= -10 && ((vacuum_25-2.7)/0.025) <= -8"
      fail: "Target pressure not reached within 30 seconds!"


- ident: Pump and valve pressure release cycle
  steps: 

    - command: pin DDTP11 output low #send signal to turn pump off
    - command: wait
      timeout: 100

    # Valve open after pump reaches pressure
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    # - command: sleepms 300


- ident: Wait for pressure to reach ambient
  steps: 
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_amb_25
    - command: eval "(vacuum_amb_25-2.7)/0.025"
      extractKey: curr_press_amb_25
    - command: eval "((vacuum_amb_25-2.7)/0.025) >= -0.3 && ((vacuum_amb_25-2.7)/0.025) <= 0"
      fail: "Ambient pressureuum not reached within 30 seconds!"


  # 50% Duty cycle
- ident: VALVE and PUMP control on startup 
  steps:
    
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    
    - command: pin DDTP11 output high #send signal to MCU to enable pump
    - command: wait
      timeout: 100

    - command: pin DDTP10 output high
    - command: wait
      timeout: 100
    - command: pin DDTP10 output low
      title: "Increment PWM duty (MCU triggered)"
    - command: wait
      timeout: 100

- ident: Target pressure reached within time frame test
  steps:
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_50
    - command: eval "(vacuum_50-2.7)/0.025"
      extractKey: curr_press_50
    - command: eval "((vacuum_50-2.7)/0.025) >= -15 && ((vacuum_50-2.7)/0.025) <= -12"
      fail: "Target pressure not reached within 30 seconds!"


- ident: Pump and valve pressure release cycle
  steps: 

    - command: pin DDTP11 output low #send signal to turn pump off
    - command: wait
      timeout: 100

    # Valve open after pump reaches pressure
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    # - command: sleepms 300


- ident: Wait for pressure to reach ambient
  steps: 
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_amb_50
    - command: eval "(vacuum_amb_50-2.7)/0.025"
      extractKey: curr_press_amb_50
    - command: eval "((vacuum_amb_50-2.7)/0.025) >= -0.3 && ((vacuum_amb_50-2.7)/0.025) <= 0"
      fail: "Ambient pressure not reached within 30 seconds!"
 

  # 75% Duty cycle
- ident: VALVE and PUMP control on startup 
  steps:
    
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    
    - command: pin DDTP11 output high #send signal to MCU to enable pump
    - command: wait
      timeout: 100

    - command: pin DDTP10 output high
    - command: wait
      timeout: 100
    - command: pin DDTP10 output low
      title: "Increment PWM duty (MCU triggered)"
    - command: wait
      timeout: 100

- ident: Target pressure reached within time frame test
  steps:
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_75
    - command: eval "(vacuum_75-2.7)/0.025"
      extractKey: curr_press_75
    - command: eval "((vacuum_75-2.7)/0.025) >= -17 && ((vacuum_75-2.7)/0.025) <= -14"
      fail: "Target pressure not reached within 30 seconds!"


- ident: Pump and valve pressure release cycle
  steps: 

    - command: pin DDTP11 output low #send signal to turn pump off
    - command: wait
      timeout: 100

    # Valve open after pump reaches pressure
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    # - command: sleepms 300


- ident: Wait for pressure to reach ambient
  steps: 
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_amb_75
    - command: eval "(vacuum_amb_75-2.7)/0.025"
      extractKey: curr_press_amb_75
    - command: eval "((vacuum_amb_75-2.7)/0.025) >= -0.5 && ((vacuum_amb_75-2.7)/0.025) <= 0"
      fail: "Ambient pressure not reached within 30 seconds!"

# 100% Duty cycle
- ident: VALVE and PUMP control on startup 
  steps:
    
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    
    - command: pin DDTP11 output high #send signal to MCU to enable pump
    - command: wait
      timeout: 100

    - command: pin DDTP10 output high
    - command: wait
      timeout: 100
    - command: pin DDTP10 output low
      title: "Increment PWM duty (MCU triggered)"
    - command: wait
      timeout: 100

- ident: Target pressure reached within time frame test
  steps:
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_100
    - command: eval "(vacuum_100-2.7)/0.025"
      extractKey: curr_press_100
    # - command: eval "((vacuum_100-2.7)/0.025) >= -10 && ((vacuum_100-2.7)/0.025) <= -8"
    #   fail: "Target pressure not reached within 30 seconds!"


- ident: Pump and valve pressure release cycle
  steps: 

    - command: pin DDTP11 output low #send signal to turn pump off
    - command: wait
      timeout: 100

    # Valve open after pump reaches pressure
    - command: pin DDTP09 output high
    - command: wait
      timeout: 300
    - command: pin DDTP09 output low
    # - command: sleepms 300


- ident: Wait for pressure to reach ambient
  steps: 
    - command: wait
      timeout: 30000
    - command: measure voltageDATP07 0-3.3V
      extractKey: vacuum_raw
    - command: eval "numeric(vacuum_raw, 'V')
      extractKey: vacuum_amb_100
    - command: eval "(vacuum_amb_100-2.7)/0.025"
      extractKey: curr_press_amb_100
    - command: eval "((vacuum_amb_100-2.7)/0.025) >= -0.5 && ((vacuum_amb_100-2.7)/0.025) <= 0"
      fail: "Ambient pressure not reached within 30 seconds!"